{
  "name": "@homegraph/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "prebuild": "prisma generate",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev --create-only",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "pnpm exec ts-node src/seed.ts",
    "studio": "prisma studio",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^6.9.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
